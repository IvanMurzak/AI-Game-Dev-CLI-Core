// Placeholder bootstrap for @baizor/gamedev-cli-core (v0.0.0).
//
// This 0.0.0 release exists only to claim the package name on npm so that
// Trusted Publishing (OIDC) can be bound to it. The real CLI-core modules —
// shared identity/pin, OAuth device login, credential store, setup-mcp and
// install-plugin logic consumed by the Unity/Unreal/Godot CLIs — land through
// this repo's build + release.yml (npm publish via OIDC), tracked by the
// auth-fixes design (tasks b1..b4).
export const version = "0.0.0";
